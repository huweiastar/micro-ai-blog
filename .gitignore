# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/.next.build/
/.next.old/
/.next.dev/
/.next.verify/
/.next.next14/
/node_modules.next14/

# CodeGraph 本地代码索引（个人工具，不入库）
/.codegraph/

# 数据库本地备份快照（异地由 rclone 推送，本地不入库）
/data/backups/
/out/

# production
# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDEA
.idea/
*.iml
.claude/

# Playwright MCP screenshots/snapshots
.playwright-mcp/

# ad-hoc debug screenshots dropped at repo root (content images live in public/)
/*.png

# runtime data (likes counts persisted at runtime)
/data/likes.json

# 文章修订快照（运行时生成）
/data/revisions/

# SQLite 运行时数据库（每日备份脚本负责备份）
/data/blog.db
/data/blog.db-wal
/data/blog.db-shm
/data/blog.db.bak

# 已迁移到 SQLite 的旧 JSON（保留磁盘文件作为迁移底档，不再跟踪）
/data/analytics.json

# 构建/发文时自动生成的产物（prebuild 与 lib/regenerate.ts 负责生成）
/public/search-index.json
/public/sitemap.xml
/public/rss.xml
/public/knowledge-index.json

# Claude Code 本地个人设置（不入库）
.claude/settings.local.json

# superpowers 插件本地目录
.superpowers/

# ── monorepo（apps/*）下的同类忽略（根部 /xxx 规则不覆盖子工作区）──
node_modules/
apps/*/.next/
apps/*/.next.build/
apps/*/.next.old/
apps/*/.next.dev/
apps/*/.next.verify/
apps/*/public/search-index.json
apps/*/public/sitemap.xml
apps/*/public/rss.xml
apps/*/public/knowledge-index.json
